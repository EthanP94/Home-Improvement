import React from 'react';
import { Link } from 'react-router-dom';

const ProjectList = ({
    _id
}) => {
    if (!_id.length) {
        return <h3>No Projects Yet</h3>
    }

    return (
        <div>
            
        </div>
    )
}